<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>method</key>
    <string>app-store-connect</string>
    <key>teamID</key>
    <string>R8953SWC87</string>
    <!-- Automatic, deliberately: this used to pin the manual "Raahe Hidayat"
         profile, which was created before Push Notifications was enabled on the
         App ID and therefore carries no aps-environment. Exporting against it
         re-signed the IPA without that entitlement, so
         registerForRemoteNotifications() failed on every shipped build and no
         APNs token was ever issued. Automatic signing lets Xcode pick a profile
         that actually satisfies aps-environment: production. -->
    <key>signingStyle</key>
    <string>automatic</string>
    <key>stripSwiftSymbols</key>
    <true/>
    <key>uploadSymbols</key>
    <true/>
</dict>
</plist>
